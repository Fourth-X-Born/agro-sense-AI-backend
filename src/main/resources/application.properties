# Application
spring.application.name=agrosense-backend

# Server
server.port=${PORT:8080}

# =========================
# Railway MySQL connection (Public URL for local dev)
# =========================
spring.datasource.url=jdbc:mysql://switchyard.proxy.rlwy.net:58811/railway
spring.datasource.username=root
spring.datasource.password=IOQXDzuwdEbPUGhjzczRApcnJWnOUMCm
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL init
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
